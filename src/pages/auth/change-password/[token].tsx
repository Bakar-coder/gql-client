import React, { FC, Fragment } from 'react'

interface tokenTypes {}

const ChangePassword: FC<tokenTypes> = ({}) => {
        return <Fragment>
            
        </Fragment>;
}

export default ChangePassword;